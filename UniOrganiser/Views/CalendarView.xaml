<UserControl x:Class="UniOrganiser.Views.CalendarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignWidth="1000" d:DesignHeight="700">
    <UserControl.Resources>
        <DataTemplate x:Key="DayCellTemplate">
            <Border BorderThickness="0.75" Padding="4" MinHeight="90">
                <Border.BorderBrush>
                    <MultiBinding Converter="{StaticResource SelectedDayBorderConverter}">
                        <Binding Path="Date" />
                        <Binding Path="DataContext.SelectedDay.Date" RelativeSource="{RelativeSource AncestorType=ItemsControl}" />
                    </MultiBinding>
                </Border.BorderBrush>
                <Border.InputBindings>
                    <MouseBinding MouseAction="LeftClick"
                                  Command="{Binding DataContext.SelectDayCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                  CommandParameter="{Binding}" />
                </Border.InputBindings>
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="Background" Value="{StaticResource PanelBrush}" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsCurrentMonth}" Value="False">
                                <Setter Property="Background" Value="{StaticResource BackgroundBrush}" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <DockPanel>
                    <TextBlock DockPanel.Dock="Top" Text="{Binding Date.Day}" FontSize="13" Margin="0,0,0,4"
                               HorizontalAlignment="Left">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsCurrentMonth}" Value="False">
                                        <Setter Property="Foreground" Value="{StaticResource MutedTextBrush}" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding IsToday}" Value="True">
                                        <Setter Property="Foreground" Value="{StaticResource AccentBrush}" />
                                        <Setter Property="FontWeight" Value="Bold" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>

                    <StackPanel>
                        <ItemsControl ItemsSource="{Binding VisibleTasks}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Button Padding="2,1" Margin="0,0,0,2" HorizontalContentAlignment="Left"
                                            Command="{Binding DataContext.EditTaskCommand, RelativeSource={RelativeSource AncestorType=ItemsControl, AncestorLevel=2}}"
                                            CommandParameter="{Binding}">
                                        <Button.Template>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="Transparent" Padding="1">
                                                    <StackPanel Orientation="Horizontal">
                                                        <Ellipse Width="7" Height="7" VerticalAlignment="Center" Margin="0,0,3,0"
                                                                 Fill="{Binding SubjectColourHex, Converter={StaticResource HexToBrushConverter}}" />
                                                        <TextBlock Text="{Binding Task.Title}" FontSize="11" TextTrimming="CharacterEllipsis"
                                                                   MaxWidth="110" />
                                                        <TextBlock Text=" ↻" FontSize="11" Foreground="{StaticResource MutedTextBrush}"
                                                                   Visibility="{Binding IsRecurring, Converter={StaticResource BoolToVisibilityConverter}}" />
                                                    </StackPanel>
                                                </Border>
                                            </ControlTemplate>
                                        </Button.Template>
                                    </Button>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                        <TextBlock Text="{Binding MoreCount, StringFormat='+{0} more'}" Style="{StaticResource MutedTextBlock}"
                                   FontSize="10"
                                   Visibility="{Binding HasMore, Converter={StaticResource BoolToVisibilityConverter}}" />
                    </StackPanel>
                </DockPanel>
            </Border>
        </DataTemplate>
    </UserControl.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,16">
            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                <Button Content="Today" Command="{Binding GoToTodayCommand}" Margin="0,0,8,0" />
                <Button Content="&lt;" Padding="10,4" Command="{Binding PreviousMonthCommand}" Margin="0,0,4,0" />
                <Button Content="&gt;" Padding="10,4" Command="{Binding NextMonthCommand}" />
            </StackPanel>
            <TextBlock Style="{StaticResource HeaderTextBlock}" Text="{Binding MonthLabel}" VerticalAlignment="Center" />
        </DockPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="16" />
                <ColumnDefinition Width="300" />
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <UniformGrid Grid.Row="0" Columns="7" Margin="0,0,0,4">
                    <TextBlock Text="Mon" Style="{StaticResource MutedTextBlock}" HorizontalAlignment="Center" />
                    <TextBlock Text="Tue" Style="{StaticResource MutedTextBlock}" HorizontalAlignment="Center" />
                    <TextBlock Text="Wed" Style="{StaticResource MutedTextBlock}" HorizontalAlignment="Center" />
                    <TextBlock Text="Thu" Style="{StaticResource MutedTextBlock}" HorizontalAlignment="Center" />
                    <TextBlock Text="Fri" Style="{StaticResource MutedTextBlock}" HorizontalAlignment="Center" />
                    <TextBlock Text="Sat" Style="{StaticResource MutedTextBlock}" HorizontalAlignment="Center" />
                    <TextBlock Text="Sun" Style="{StaticResource MutedTextBlock}" HorizontalAlignment="Center" />
                </UniformGrid>

                <ItemsControl Grid.Row="1" ItemsSource="{Binding Days}" ItemTemplate="{StaticResource DayCellTemplate}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <UniformGrid Columns="7" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                </ItemsControl>
            </Grid>

            <Border Grid.Column="2" Background="{StaticResource PanelBrush}" BorderBrush="{StaticResource BorderBrush}"
                    BorderThickness="1" CornerRadius="4" Padding="16">
                <DockPanel>
                    <StackPanel DockPanel.Dock="Top" Margin="0,0,0,12">
                        <TextBlock Text="{Binding SelectedDay.Date, StringFormat='dddd, d MMMM'}" FontSize="15" FontWeight="SemiBold"
                                   Margin="0,0,0,8" />
                        <Button Content="+ Add Task" Style="{StaticResource AccentButton}"
                                HorizontalAlignment="Left" Command="{Binding AddTaskCommand}" />
                    </StackPanel>

                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <StackPanel>
                            <TextBlock Text="No tasks on this day.">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock" BasedOn="{StaticResource MutedTextBlock}">
                                        <Setter Property="Visibility" Value="Collapsed" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding SelectedDay.TasksOnDay.Count}" Value="0">
                                                <Setter Property="Visibility" Value="Visible" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>

                            <ItemsControl ItemsSource="{Binding SelectedDay.TasksOnDay}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrush}"
                                                BorderThickness="1" CornerRadius="4" Padding="10,8" Margin="0,0,0,8">
                                            <StackPanel>
                                                <StackPanel Orientation="Horizontal">
                                                    <Ellipse Width="10" Height="10" VerticalAlignment="Center" Margin="0,0,8,0"
                                                             Fill="{Binding SubjectColourHex, Converter={StaticResource HexToBrushConverter}}" />
                                                    <TextBlock Text="{Binding Task.Title}" FontSize="13" TextWrapping="Wrap" />
                                                    <TextBlock Text=" ↻" FontSize="12" Foreground="{StaticResource MutedTextBrush}"
                                                               Visibility="{Binding IsRecurring, Converter={StaticResource BoolToVisibilityConverter}}" />
                                                </StackPanel>
                                                <TextBlock Style="{StaticResource MutedTextBlock}" Margin="18,2,0,6"
                                                           Text="{Binding Task.DueTime, StringFormat='{}{0:hh\\:mm}'}" />
                                                <StackPanel Orientation="Horizontal" Margin="18,0,0,0">
                                                    <Button Content="Edit" Padding="8,3" FontSize="11"
                                                            Command="{Binding DataContext.EditTaskCommand, RelativeSource={RelativeSource AncestorType=ItemsControl, AncestorLevel=1}}"
                                                            CommandParameter="{Binding}" />
                                                    <Button Content="Delete" Padding="8,3" FontSize="11" Margin="6,0,0,0"
                                                            Command="{Binding DataContext.DeleteTaskCommand, RelativeSource={RelativeSource AncestorType=ItemsControl, AncestorLevel=1}}"
                                                            CommandParameter="{Binding}" />
                                                </StackPanel>
                                            </StackPanel>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </ScrollViewer>
                </DockPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
