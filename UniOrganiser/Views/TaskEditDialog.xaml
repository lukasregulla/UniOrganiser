<Window x:Class="UniOrganiser.Views.TaskEditDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Style="{StaticResource {x:Type Window}}"
        Title="{Binding DialogTitle}" Height="720" Width="420"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        ShowInTaskbar="False">
    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="24">
        <StackPanel>
            <TextBlock Text="{Binding DialogTitle}" Style="{StaticResource HeaderTextBlock}" Margin="0,0,0,16" />

            <TextBlock Text="Title" Style="{StaticResource MutedTextBlock}" Margin="0,0,0,4" />
            <TextBox Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12" />

            <TextBlock Text="Description (optional)" Style="{StaticResource MutedTextBlock}" Margin="0,0,0,4" />
            <TextBox Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}"
                     AcceptsReturn="True" TextWrapping="Wrap" Height="60" Margin="0,0,0,12" />

            <TextBlock Text="Subject" Style="{StaticResource MutedTextBlock}" Margin="0,0,0,4" />
            <ComboBox ItemsSource="{Binding SubjectOptions}" SelectedItem="{Binding SelectedSubjectOption}"
                      DisplayMemberPath="Name" Margin="0,0,0,12" />

            <Grid Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="12" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="Due date" Style="{StaticResource MutedTextBlock}" Margin="0,0,0,4" />
                    <DatePicker SelectedDate="{Binding DueDate}" />
                </StackPanel>

                <StackPanel Grid.Column="2">
                    <TextBlock Text="Due time (optional, HH:mm)" Style="{StaticResource MutedTextBlock}" Margin="0,0,0,4" />
                    <TextBox Text="{Binding DueTimeText, UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>
            </Grid>

            <TextBlock Text="Priority" Style="{StaticResource MutedTextBlock}" Margin="0,0,0,4" />
            <ComboBox ItemsSource="{Binding PriorityOptions}" SelectedItem="{Binding Priority}" Margin="0,0,0,12" />

            <CheckBox Content="Completed" IsChecked="{Binding IsCompleted}" Margin="0,0,0,12" />

            <Border BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0" Margin="0,0,0,12" />

            <TextBlock Text="Repeat" Style="{StaticResource MutedTextBlock}" Margin="0,0,0,4" />
            <ComboBox ItemsSource="{Binding RepeatOptions}" SelectedItem="{Binding SelectedRepeatOption}" Margin="0,0,0,12" />

            <StackPanel Visibility="{Binding ShowDaysPicker, Converter={StaticResource BoolToVisibilityConverter}}"
                        Margin="0,0,0,12">
                <TextBlock Text="On these days" Style="{StaticResource MutedTextBlock}" Margin="0,0,0,4" />
                <ItemsControl ItemsSource="{Binding DayToggles}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <CheckBox Content="{Binding Label}" IsChecked="{Binding IsSelected}" Margin="0,0,12,0" />
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>

            <StackPanel Visibility="{Binding ShowRepeatEndDate, Converter={StaticResource BoolToVisibilityConverter}}"
                        Margin="0,0,0,12">
                <TextBlock Text="Repeat ends (optional)" Style="{StaticResource MutedTextBlock}" Margin="0,0,0,4" />
                <DatePicker SelectedDate="{Binding RepeatEndDate}" />
            </StackPanel>

            <StackPanel Visibility="{Binding IsPartOfExistingSeries, Converter={StaticResource BoolToVisibilityConverter}}"
                        Margin="0,0,0,12">
                <TextBlock Text="Apply changes to" Style="{StaticResource MutedTextBlock}" Margin="0,0,0,4" />
                <RadioButton Content="This occurrence only" GroupName="Scope" HorizontalAlignment="Left"
                             IsChecked="{Binding ApplyToAllFuture, Converter={StaticResource InverseBooleanConverter}}" />
                <RadioButton Content="This and all future occurrences" GroupName="Scope" HorizontalAlignment="Left" Margin="0,4,0,0"
                             IsChecked="{Binding ApplyToAllFuture}" />
            </StackPanel>

            <TextBlock Text="{Binding ValidationError}" Foreground="#E06C75" TextWrapping="Wrap" Margin="0,0,0,12"
                       Visibility="{Binding ValidationError, Converter={StaticResource NullToVisibilityConverter}}" />

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Cancel" Command="{Binding CancelCommand}" Margin="0,0,8,0" />
                <Button Content="Save" Style="{StaticResource AccentButton}" Command="{Binding SaveCommand}" />
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</Window>
