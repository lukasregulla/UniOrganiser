<UserControl x:Class="UniOrganiser.Views.SubjectsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignWidth="700" d:DesignHeight="500">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,16">
            <Button DockPanel.Dock="Right" Style="{StaticResource AccentButton}"
                    Content="+ Add Subject" Command="{Binding AddNewCommand}" />
            <TextBlock Style="{StaticResource HeaderTextBlock}" Text="Subjects" VerticalAlignment="Center" />
        </DockPanel>

        <Grid Grid.Row="1">
            <TextBlock Text="No subjects yet. Add one to start tagging your tasks."
                       HorizontalAlignment="Left" VerticalAlignment="Top" Margin="0,12,0,0">
                <TextBlock.Style>
                    <Style TargetType="TextBlock" BasedOn="{StaticResource MutedTextBlock}">
                        <Setter Property="Visibility" Value="Collapsed" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Subjects.Count}" Value="0">
                                <Setter Property="Visibility" Value="Visible" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>

            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding Subjects}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="{StaticResource PanelBrush}" BorderBrush="{StaticResource BorderBrush}"
                                    BorderThickness="1" CornerRadius="4" Padding="12,10" Margin="0,0,0,8">
                                <DockPanel>
                                    <Ellipse DockPanel.Dock="Left" Width="14" Height="14" VerticalAlignment="Center"
                                             Margin="0,0,12,0"
                                             Fill="{Binding ColourHex, Converter={StaticResource HexToBrushConverter}}" />
                                    <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                                        <Button Content="Edit" Padding="10,4"
                                                Command="{Binding DataContext.EditCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                CommandParameter="{Binding}" />
                                        <Button Content="Delete" Padding="10,4" Margin="8,0,0,0"
                                                Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                CommandParameter="{Binding}" />
                                    </StackPanel>
                                    <TextBlock Text="{Binding Name}" VerticalAlignment="Center" FontSize="14" />
                                </DockPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>

        <!-- Editor overlay -->
        <Border Grid.Row="0" Grid.RowSpan="2" Background="#CC000000"
                Visibility="{Binding IsEditorOpen, Converter={StaticResource BoolToVisibilityConverter}}">
            <Border Background="{StaticResource PanelBrush}" BorderBrush="{StaticResource BorderBrush}"
                    BorderThickness="1" CornerRadius="6" Padding="20" Width="360"
                    HorizontalAlignment="Center" VerticalAlignment="Center">
                <StackPanel>
                    <TextBlock Text="{Binding EditorTitle}" Style="{StaticResource HeaderTextBlock}" FontSize="16" Margin="0,0,0,12" />

                    <TextBlock Text="Name" Style="{StaticResource MutedTextBlock}" Margin="0,0,0,4" />
                    <TextBox Text="{Binding EditingName, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12" />

                    <TextBlock Text="Colour" Style="{StaticResource MutedTextBlock}" Margin="0,0,0,4" />
                    <ItemsControl ItemsSource="{Binding Palette}" Margin="0,0,0,8">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Width="32" Height="32" Margin="4" CornerRadius="16"
                                        Background="{Binding Converter={StaticResource HexToBrushConverter}}"
                                        BorderThickness="2" Cursor="Hand">
                                    <Border.BorderBrush>
                                        <MultiBinding Converter="{StaticResource ColourSelectedConverter}">
                                            <Binding />
                                            <Binding Path="DataContext.EditingColourHex" RelativeSource="{RelativeSource AncestorType=ItemsControl}" />
                                        </MultiBinding>
                                    </Border.BorderBrush>
                                    <Border.InputBindings>
                                        <MouseBinding MouseAction="LeftClick"
                                                      Command="{Binding DataContext.SelectColourCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                      CommandParameter="{Binding}" />
                                    </Border.InputBindings>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <TextBlock Text="{Binding ValidationError}" Foreground="#E06C75" Margin="0,0,0,8" TextWrapping="Wrap"
                               Visibility="{Binding ValidationError, Converter={StaticResource NullToVisibilityConverter}}" />

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="Cancel" Command="{Binding CancelCommand}" Margin="0,0,8,0" />
                        <Button Content="Save" Style="{StaticResource AccentButton}" Command="{Binding SaveCommand}" />
                    </StackPanel>
                </StackPanel>
            </Border>
        </Border>
    </Grid>
</UserControl>
