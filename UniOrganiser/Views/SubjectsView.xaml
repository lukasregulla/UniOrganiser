<UserControl x:Class="UniOrganiser.Views.SubjectsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             mc:Ignorable="d"
             d:DesignWidth="700" d:DesignHeight="500">
    <Grid>
        <Grid Margin="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <DockPanel Grid.Row="0" Margin="0,0,0,16">
                <ui:Button DockPanel.Dock="Right"
                           Appearance="Primary"
                           Content="Add Subject"
                           Command="{Binding AddNewCommand}">
                    <ui:Button.Icon>
                        <ui:SymbolIcon Symbol="Add24" />
                    </ui:Button.Icon>
                </ui:Button>
                <TextBlock Style="{StaticResource SubtitleTextBlockStyle}" Text="Subjects" VerticalAlignment="Center" />
            </DockPanel>

            <Grid Grid.Row="1">
                <TextBlock Text="No subjects yet. Add one to start tagging your tasks."
                           HorizontalAlignment="Left" VerticalAlignment="Top" Margin="0,16,0,0"
                           Foreground="{DynamicResource TextFillColorSecondaryBrush}">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock" BasedOn="{StaticResource BodyTextBlockStyle}">
                            <Setter Property="Visibility" Value="Collapsed" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Subjects.Count}" Value="0">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>

                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding Subjects}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <ui:Card Margin="0,0,0,8" Padding="16">
                                    <DockPanel>
                                        <Ellipse DockPanel.Dock="Left" Width="16" Height="16" VerticalAlignment="Center"
                                                 Margin="0,0,16,0"
                                                 Fill="{Binding ColourHex, Converter={StaticResource HexToBrushConverter}}" />
                                        <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" VerticalAlignment="Center">
                                            <ui:Button Appearance="Secondary"
                                                       ToolTip="Edit"
                                                       AutomationProperties.Name="Edit subject"
                                                       Command="{Binding DataContext.EditCommand, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}}"
                                                       CommandParameter="{Binding}">
                                                <ui:Button.Icon>
                                                    <ui:SymbolIcon Symbol="Edit24" />
                                                </ui:Button.Icon>
                                            </ui:Button>
                                            <ui:Button Appearance="Secondary"
                                                       ToolTip="Delete"
                                                       AutomationProperties.Name="Delete subject"
                                                       Margin="8,0,0,0"
                                                       Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}}"
                                                       CommandParameter="{Binding}">
                                                <ui:Button.Icon>
                                                    <ui:SymbolIcon Symbol="Delete24" />
                                                </ui:Button.Icon>
                                            </ui:Button>
                                        </StackPanel>
                                        <TextBlock Text="{Binding Name}"
                                                   Style="{StaticResource BodyTextBlockStyle}"
                                                   VerticalAlignment="Center" />
                                    </DockPanel>
                                </ui:Card>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>
        </Grid>

        <!-- Editor overlay -->
        <Border Background="{DynamicResource SmokeFillColorDefaultBrush}"
                Visibility="{Binding IsEditorOpen, Converter={StaticResource BoolToVisibilityConverter}}">
            <!-- ContentDialogBackground is the surface WPF-UI uses for its own modal dialogs;
                 the card brushes are too close to the page background to read as modal. -->
            <Border Background="{DynamicResource ContentDialogBackground}"
                    BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="{DynamicResource OverlayCornerRadius}"
                    Padding="24" Width="360"
                    HorizontalAlignment="Center" VerticalAlignment="Center">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="32" ShadowDepth="8" Direction="270" Opacity="0.5" Color="Black" />
                </Border.Effect>
                <StackPanel>
                    <TextBlock Text="{Binding EditorTitle}"
                               Style="{StaticResource SubtitleTextBlockStyle}"
                               Margin="0,0,0,16" />

                    <TextBlock Text="Name"
                               Style="{StaticResource CaptionTextBlockStyle}"
                               Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                               Margin="0,0,0,8" />
                    <ui:TextBox Text="{Binding EditingName, UpdateSourceTrigger=PropertyChanged}"
                                PlaceholderText="e.g. Databases"
                                Margin="0,0,0,16" />

                    <TextBlock Text="Colour"
                               Style="{StaticResource CaptionTextBlockStyle}"
                               Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                               Margin="0,0,0,8" />
                    <ItemsControl ItemsSource="{Binding Palette}" Margin="0,0,0,16">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Width="32" Height="32" Margin="0,0,8,8" CornerRadius="16"
                                        Background="{Binding Converter={StaticResource HexToBrushConverter}}"
                                        BorderThickness="2" Cursor="Hand">
                                    <Border.InputBindings>
                                        <MouseBinding MouseAction="LeftClick"
                                                      Command="{Binding DataContext.SelectColourCommand, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}}"
                                                      CommandParameter="{Binding}" />
                                    </Border.InputBindings>
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="BorderBrush" Value="Transparent" />
                                            <Style.Triggers>
                                                <DataTrigger Value="True">
                                                    <DataTrigger.Binding>
                                                        <MultiBinding Converter="{StaticResource ColourSelectedConverter}">
                                                            <Binding />
                                                            <Binding Path="DataContext.EditingColourHex"
                                                                     RelativeSource="{RelativeSource AncestorType={x:Type UserControl}}" />
                                                        </MultiBinding>
                                                    </DataTrigger.Binding>
                                                    <Setter Property="BorderBrush"
                                                            Value="{DynamicResource AccentFillColorDefaultBrush}" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <TextBlock Text="{Binding ValidationError}"
                               Style="{StaticResource CaptionTextBlockStyle}"
                               Foreground="{DynamicResource SystemFillColorCriticalBrush}"
                               Margin="0,0,0,16" TextWrapping="Wrap"
                               Visibility="{Binding ValidationError, Converter={StaticResource NullToVisibilityConverter}}" />

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <ui:Button Content="Cancel" Command="{Binding CancelCommand}" Margin="0,0,8,0" />
                        <ui:Button Content="Save" Appearance="Primary" Command="{Binding SaveCommand}" />
                    </StackPanel>
                </StackPanel>
            </Border>
        </Border>
    </Grid>
</UserControl>
