<UserControl x:Class="UniOrganiser.Views.TaskListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:b="clr-namespace:UniOrganiser.Behaviours"
             mc:Ignorable="d"
             d:DesignWidth="800" d:DesignHeight="600">
    <Grid Margin="24">
        <Grid.Resources>
            <Style x:Key="FilterRowLabel" TargetType="TextBlock" BasedOn="{StaticResource CaptionTextBlockStyle}">
                <Setter Property="Foreground" Value="{DynamicResource TextFillColorSecondaryBrush}" />
                <Setter Property="VerticalAlignment" Value="Center" />
                <Setter Property="Width" Value="72" />
                <Setter Property="Margin" Value="0,0,16,0" />
            </Style>

            <CollectionViewSource x:Key="GroupedTasks" Source="{Binding Items}">
                <CollectionViewSource.GroupDescriptions>
                    <PropertyGroupDescription PropertyName="WeekLabel" />
                </CollectionViewSource.GroupDescriptions>
            </CollectionViewSource>
        </Grid.Resources>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,16">
            <ui:Button DockPanel.Dock="Right"
                       Appearance="Primary"
                       Content="Add Task"
                       Command="{Binding AddTaskCommand}">
                <ui:Button.Icon>
                    <ui:SymbolIcon Symbol="Add24" />
                </ui:Button.Icon>
            </ui:Button>

            <ui:ToggleSwitch DockPanel.Dock="Right"
                             Content="Show completed"
                             Margin="0,0,16,0"
                             VerticalAlignment="Center"
                             IsChecked="{Binding ShowCompleted}" />

            <TextBlock Style="{StaticResource SubtitleTextBlockStyle}" Text="Tasks" VerticalAlignment="Center" />
        </DockPanel>

        <DockPanel Grid.Row="1" Margin="0,0,0,8">
            <TextBlock DockPanel.Dock="Left"
                       Text="Subjects"
                       Style="{StaticResource FilterRowLabel}" />
            <ItemsControl ItemsSource="{Binding SubjectToggles}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <CheckBox Margin="0,0,16,0" IsChecked="{Binding IsSelected}">
                            <StackPanel Orientation="Horizontal">
                                <Ellipse Width="10" Height="10" VerticalAlignment="Center" Margin="0,0,8,0"
                                         Fill="{Binding ColourHex, Converter={StaticResource HexToBrushConverter}}" />
                                <TextBlock Text="{Binding Name}" VerticalAlignment="Center" />
                            </StackPanel>
                        </CheckBox>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </DockPanel>

        <DockPanel Grid.Row="2" Margin="0,0,0,16">
            <TextBlock DockPanel.Dock="Left"
                       Text="Categories"
                       Style="{StaticResource FilterRowLabel}" />
            <ItemsControl ItemsSource="{Binding CategoryToggles}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <CheckBox Margin="0,0,16,0" IsChecked="{Binding IsSelected}">
                            <StackPanel Orientation="Horizontal">
                                <Border Width="10" Height="10" VerticalAlignment="Center" Margin="0,0,8,0"
                                        CornerRadius="2"
                                        Background="{Binding ColourHex, Converter={StaticResource HexToBrushConverter}}" />
                                <TextBlock Text="{Binding Name}" VerticalAlignment="Center" />
                            </StackPanel>
                        </CheckBox>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </DockPanel>

        <Grid Grid.Row="3">
            <!-- Two empty states: an AND-combined filter pair makes "no matches"
                 easy to hit, and that reads very differently from "no tasks". -->
            <TextBlock Text="No tasks yet. Add one to get started."
                       HorizontalAlignment="Left" VerticalAlignment="Top" Margin="0,16,0,0"
                       Foreground="{DynamicResource TextFillColorSecondaryBrush}">
                <TextBlock.Style>
                    <Style TargetType="TextBlock" BasedOn="{StaticResource BodyTextBlockStyle}">
                        <Setter Property="Visibility" Value="Collapsed" />
                        <Style.Triggers>
                            <MultiDataTrigger>
                                <MultiDataTrigger.Conditions>
                                    <Condition Binding="{Binding Items.Count}" Value="0" />
                                    <Condition Binding="{Binding HasActiveFilters}" Value="False" />
                                </MultiDataTrigger.Conditions>
                                <Setter Property="Visibility" Value="Visible" />
                            </MultiDataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>

            <TextBlock Text="No tasks match the current filters."
                       HorizontalAlignment="Left" VerticalAlignment="Top" Margin="0,16,0,0"
                       Foreground="{DynamicResource TextFillColorSecondaryBrush}">
                <TextBlock.Style>
                    <Style TargetType="TextBlock" BasedOn="{StaticResource BodyTextBlockStyle}">
                        <Setter Property="Visibility" Value="Collapsed" />
                        <Style.Triggers>
                            <MultiDataTrigger>
                                <MultiDataTrigger.Conditions>
                                    <Condition Binding="{Binding Items.Count}" Value="0" />
                                    <Condition Binding="{Binding HasActiveFilters}" Value="True" />
                                </MultiDataTrigger.Conditions>
                                <Setter Property="Visibility" Value="Visible" />
                            </MultiDataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>

            <ScrollViewer VerticalScrollBarVisibility="Auto" b:SmoothScroll.IsEnabled="True">
                <ItemsControl ItemsSource="{Binding Source={StaticResource GroupedTasks}}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <ui:Card Margin="0,0,0,8" Padding="16">
                                <DockPanel>
                                    <CheckBox DockPanel.Dock="Left" VerticalAlignment="Center" Margin="0,0,8,0"
                                              IsChecked="{Binding Task.IsCompleted, Mode=OneWay}"
                                              Command="{Binding DataContext.ToggleCompleteCommand, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}}"
                                              CommandParameter="{Binding}" />

                                    <Ellipse DockPanel.Dock="Left" Width="12" Height="12" VerticalAlignment="Center"
                                             Margin="0,0,16,0"
                                             Fill="{Binding SubjectColourHex, Converter={StaticResource HexToBrushConverter}}" />

                                    <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" VerticalAlignment="Center">
                                        <ui:Button Appearance="Secondary"
                                                   ToolTip="Edit"
                                                   AutomationProperties.Name="Edit task"
                                                   Command="{Binding DataContext.EditTaskCommand, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}}"
                                                   CommandParameter="{Binding}">
                                            <ui:Button.Icon>
                                                <ui:SymbolIcon Symbol="Edit24" />
                                            </ui:Button.Icon>
                                        </ui:Button>
                                        <ui:Button Appearance="Secondary"
                                                   ToolTip="Delete"
                                                   AutomationProperties.Name="Delete task"
                                                   Margin="8,0,0,0"
                                                   Command="{Binding DataContext.DeleteTaskCommand, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}}"
                                                   CommandParameter="{Binding}">
                                            <ui:Button.Icon>
                                                <ui:SymbolIcon Symbol="Delete24" />
                                            </ui:Button.Icon>
                                        </ui:Button>
                                    </StackPanel>

                                    <StackPanel VerticalAlignment="Center">
                                        <TextBlock Text="{Binding Task.Title}" TextTrimming="CharacterEllipsis">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock" BasedOn="{StaticResource BodyTextBlockStyle}">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Task.IsCompleted}" Value="True">
                                                            <Setter Property="TextDecorations" Value="Strikethrough" />
                                                            <Setter Property="Foreground"
                                                                    Value="{DynamicResource TextFillColorSecondaryBrush}" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                        <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                                            <StackPanel.Resources>
                                                <Style TargetType="TextBlock" BasedOn="{StaticResource CaptionTextBlockStyle}">
                                                    <Setter Property="Foreground"
                                                            Value="{DynamicResource TextFillColorSecondaryBrush}" />
                                                    <Setter Property="Margin" Value="0,0,16,0" />
                                                    <Setter Property="VerticalAlignment" Value="Center" />
                                                </Style>
                                            </StackPanel.Resources>
                                            <!-- Collapse the host too, else the margin leaves a
                                                 gap on uncategorised tasks. -->
                                            <ContentControl Content="{Binding}"
                                                            ContentTemplate="{StaticResource CategoryPillTemplate}"
                                                            Margin="0,0,16,0"
                                                            VerticalAlignment="Center"
                                                            Visibility="{Binding HasCategory, Converter={StaticResource BoolToVisibilityConverter}}" />
                                            <TextBlock Text="{Binding SubjectName, TargetNullValue='No subject'}" />
                                            <TextBlock Text="{Binding Task.DueDate, StringFormat='ddd, d MMM'}" Margin="0,0,8,0" />
                                            <TextBlock Text="{Binding Task.DueTime, StringFormat='{}{0:hh\\:mm}'}" />
                                            <TextBlock Text="{Binding Task.Priority}" />
                                            <ui:SymbolIcon Symbol="ArrowRepeatAll24"
                                                           FontSize="14"
                                                           VerticalAlignment="Center"
                                                           Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                                           ToolTip="Repeating task"
                                                           Visibility="{Binding IsRecurring, Converter={StaticResource BoolToVisibilityConverter}}" />
                                        </StackPanel>
                                    </StackPanel>
                                </DockPanel>
                            </ui:Card>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                    <ItemsControl.GroupStyle>
                        <GroupStyle>
                            <GroupStyle.HeaderTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Name}"
                                               Style="{StaticResource BodyStrongTextBlockStyle}"
                                               Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                               Margin="0,16,0,8" />
                                </DataTemplate>
                            </GroupStyle.HeaderTemplate>
                        </GroupStyle>
                    </ItemsControl.GroupStyle>
                </ItemsControl>
            </ScrollViewer>
        </Grid>
    </Grid>
</UserControl>
